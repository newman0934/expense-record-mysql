<div>
  <div class="row my-5">
    <a href="/records/new" class="btn btn-outline-primary col-sm-2">新增紀錄</a>
    <div class="col-sm-6 text-center">總金額：
      <span class="text-warning h2">{{{totalAmount}}}</span>
    </div>
<div class="col-sm-2 justify-content-end" style="display: flex">
      <div class="btn-group">
       <button type="button" class="btn btn-outline-secondary dropdown-toggle monthDropdownBtn" data-toggle="dropdown"
      aria-haspopup="true" aria-expanded="false" data-item="{{filterMonth}}">
      {{#if filterMonth}}
      {{{filterMonth}}} 月份
      {{else}}
      不按月份
      {{/if}}
    </button>
    <div class="dropdown-menu">
      <a class="dropdown-item" href="/?filterMonth=&filterCategory={{filterCategory}}">不按月份</a>
      <a class="dropdown-item" href="/?filterMonth=1&filterCategory={{filterCategory}}">1月份</a>
      <a class="dropdown-item" href="/?filterMonth=2&filterCategory={{filterCategory}}">2月份</a>
      <a class="dropdown-item" href="/?filterMonth=3&filterCategory={{filterCategory}}">3月份</a>
      <a class="dropdown-item" href="/?filterMonth=4&filterCategory={{filterCategory}}">4月份</a>
      <a class="dropdown-item" href="/?filterMonth=5&filterCategory={{filterCategory}}">5月份</a>
      <a class="dropdown-item" href="/?filterMonth=6&filterCategory={{filterCategory}}">6月份</a>
      <a class="dropdown-item" href="/?filterMonth=7&filterCategory={{filterCategory}}">7月份</a>
      <a class="dropdown-item" href="/?filterMonth=8&filterCategory={{filterCategory}}">8月份</a>
      <a class="dropdown-item" href="/?filterMonth=9&filterCategory={{filterCategory}}">9月份</a>
      <a class="dropdown-item" href="/?filterMonth=10&filterCategory={{filterCategory}}">10月份</a>
      <a class="dropdown-item" href="/?filterMonth=11&filterCategory={{filterCategory}}">11月份</a>
      <a class="dropdown-item" href="/?filterMonth=12&filterCategory={{filterCategory}}">12月份</a>
    </div>
      </div>
    </div>

    <div class="col-sm-2 justify-content-end" style="display: flex">
      <div class="btn-group">
        <button type="button" class="btn btn-outline-secondary dropdown-toggle ml-2 categoryDropdownBtn" data-toggle="dropdown" aria-haspopup="true"
          aria-expanded="false" data-item="{{filterCategory}}">
          {{#if filterCategory}} {{{categoryChinese}}} 類別 {{else}} 不按類別 {{/if}}
        </button>
        <div class="dropdown-menu">
          <a class="dropdown-item" href="/?filterMonth={{filterMonth}}&filterCategory=">不按類別</a>
          <a class="dropdown-item" href="/?filterMonth={{filterMonth}}&filterCategory=home">家居物業</a>
          <a class="dropdown-item" href="/?filterMonth={{filterMonth}}&filterCategory=shuttle-van">交通出行</a>
          <a class="dropdown-item" href="/?filterMonth={{filterMonth}}&filterCategory=grin-beam">休閒娛樂</a>
          <a class="dropdown-item" href="/?filterMonth={{filterMonth}}&filterCategory=utensils">餐飲食品</a>
          <a class="dropdown-item" href="/?filterMonth={{filterMonth}}&filterCategory=pen">其他</a>
        </div>
      </div>
    </div>
  </div>
</div>

<table class="table table-striped table-hover">
  <tbody>
    {{#each records}}
    <tr>
      <td style="width:5%">
        <i class="fas fa-{{{this.category}}} h1 text-primary text-center py-1"></i>
      </td>
      <td style="width:30%">
        <div class="d-inline-block text-left">
          <h4>{{{this.name}}}</h4>
          <small>{{{dateFormat this.date}}}</small>
        </div>
      </td>
            <td>
        <div class="d-inline-block text-left">
          <h4>{{{this.merchant}}}</h4>
        </div>
      </td>
      <td style="vertical-align:middle;">
        <div>
          <h4 class="align-items-center">{{{this.amount}}}</h4>
      </td>
      <td class="text-right" style="vertical-align:middle;">
        <a href="/records/{{{this._id}}}/edit" class="btn btn-outline-success">修改</a>
        <button type="button" class="btn btn-outline-danger" data-toggle="modal" data-target="#delete-{{this._id}}">
          刪除
        </button>
      </td>
    </tr>
    <div class="modal fade" id="delete-{{this._id}}" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="deleteModalLabel">刪除確認視窗</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          <div class="modal-body">
            請問是否要刪除這筆紀錄
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-outline-secondary" data-dismiss="modal">取消</button>
            <form action="/records/{{this._id}}/delete?_method=DELETE" method="POST" style="display: inline;">
              <button type="submit" class="btn btn-outline-danger">刪除</button>
            </form>
          </div>
        </div>
      </div>
    </div>
    {{/each}}
  </tbody>
</table>